# Production base URL — used for generating Collab and View share links.
# This file IS committed to git (not in .gitignore).
# It is safe to commit because VITE_APP_URL is not a secret.
# For local dev, this value is ignored — window.location.origin is used as fallback.
VITE_APP_URL=https://stage.tooawake.online
